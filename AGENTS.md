# AGENTS.md

## Cursor Cloud specific instructions

This repository (`any-roz`) is currently an empty/blank repository with only a `README.md` file. There are no services, dependencies, build steps, lint checks, or tests to run.

When code is added to this repository, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during setup
